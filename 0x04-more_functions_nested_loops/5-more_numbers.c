#include "main.h"

/**
 * void more_numbers - function that prints 10
 * times the numbers,
 * from 0 to 14, followed by a new line.
 *
 * Return: Always (0)
 */
void more_numbers(void)
{
	char = '0';
	int i;

	for (i = 0; i <= 10)
	{
		while (c < '14')
		{
			_putchar("%d", c);
			c++
		}
	}
	_putchar('\n');
}

